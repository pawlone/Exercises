import Row from 'react-bootstrap/Row';

const OutputScreen = ({ children }) => {
    return (
        <Row className='justify-content-center'>{children}</Row>   
    )
};

export default OutputScreen;



